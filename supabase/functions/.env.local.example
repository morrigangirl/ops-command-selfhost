# Gemini configuration (server-side only)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-pro

# CORS allowlist for browser origins calling your edge functions
ALLOWED_ORIGINS=https://app.example.com,http://localhost:8200
